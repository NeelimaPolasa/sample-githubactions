name: Docker

on: 
   push:
     branches:
       - main
      

jobs:
  build:
    name: Build Container image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2


          
      
