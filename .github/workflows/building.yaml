name: docker_build_push_acr
 
on:
  push:
    branches:
      - feature/*
 
jobs:
  docker_build_push_acr:
    name: 'Docker Build and Push to ACR'
    runs-on: ubuntu-latest
    environment: production
  
    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    defaults:
      run:
        shell: bash
  
    steps:
    # Checkout the repository to the GitHub Actions runner
    - name: Checkout
      uses: actions/checkout@v2
      
    - name: 'Docker Login'
      uses: azure/docker-login@v1
      with:
        login-server: githubactionsneel.azurecr.io
        username: githubactionsneel
        password: A4BccVelV8JsFcEBXC1BxPZFWTmvdgZwCks2vuK8Jw+ACRDLVoIX
    - name: Build & Push
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: githubactionsneel.azurecr.io/aspcoresample:${{ github.sha }}
 #  - name: Build the frontend image and push it to ACR
 #     uses: docker/build-push-action@v2
 #    with:
 #      push: true
 #      tags: githubactionsneel.azurecr.io/aspcoresample:${{ github.sha }}
 #       file: Dockerfile
